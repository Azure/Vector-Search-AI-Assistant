apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chat-service-web-api.fullname" . }}-test-connection"
  labels:
    {{- include "chat-service-web-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chat-service-web-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
