apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-service-web-api.fullname" . }}
  labels:
    {{- include "chat-service-web-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chat-service-web-api.selectorLabels" . | nindent 4 }}
